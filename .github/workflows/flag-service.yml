name: CI - flag-service

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "flag-service/**"
  pull_request:
    branches: [main]
    paths:
      - "flag-service/**"

permissions:
  contents: write
  id-token: write

jobs:
  ci:
    uses: ./.github/workflows/_ci-python.yml
    with:
      service: flag-service
      ecr_repo: flag-service
    secrets: inherit
